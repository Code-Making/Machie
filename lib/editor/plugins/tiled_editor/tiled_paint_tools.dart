// lib/editor/plugins/tiled_editor/tiled_paint_tools.dart

enum TiledPaintMode { paint, fill, erase, select, move }

enum TiledEditorMode { panZoom, paint, object }

enum ObjectTool {
  select,
  move,
  addRectangle,
  addEllipse,
  addPoint,
  addPolygon,
  addPolyline,
  addText,
}
